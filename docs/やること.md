# やること（保留中の作業）

- 最終更新: 2026-08-18

**着手できないものは、なぜ着手できないかを必ず書く。**
「あとでやる」とだけ書いた項目は、理由が消えた瞬間に忘れられる。

---

## 待ちになっているもの

| # | やること | 何を待っているか |
|---|---|---|
| T-1 | ~~塗り絵の台紙との照合~~ → **繋いだ**（F-214）。絵に `species` が付き、頭の向きも台紙の正解を使う | 済 |
| T-12 | ~~生き物ごとの動き~~ → **入った**（F-215〜F-217）。実機での見え方と fps は未確認 | 会場PC |
| T-2 | Windows 実機でのリハーサル | 会場PC（または同等の1台）。`docs/リハーサル手順.md` |
| T-3 | fps の実測 | 同上。**開発環境（GPU無効）では原理的に測れない唯一の項目**。いまの実測は 11.8〜12.6fps（絵11枚）。背景をパーツ画像にしても図形のときと変わらなかったので、**下げるつまみは「背景の強さ」しか残っていない**。実機で 30fps を超えるかが判断の分かれ目 |
| T-15 | 企画書の空欄を埋める | 会場名・日付・想定来場者数・費用・モニターの手配。`docs/企画書.md` の 〈 〉 の部分 |
| T-14 | **更新の確認ボタンを実機で試す** | Windows の実機とネット接続。開発環境では「開発中の起動では確認できません」で止まるため、**押して本当に入れ替わるところまでは未検証** |
| T-4 | 切り抜きのしきい値の最終確認 | 会場で使う紙とスキャナ。自動選びは入れたが（F-209）、標本は3枚しかない。**スキャナの模擬（A4・150/300dpi）では正しく抜けた**ので、残るのは実物の紙の色 |

### T-1 に必要なもの

1. ~~台紙の線画~~ → **6種そろっている**（`assets/templates/`）。
   輪郭は全部閉じていて、見分けの試験も 48/48 で通っている。
   ただし**印刷には解像度が足りない**（376〜500px。A4でギザギザになる）ので、
   長辺2000px以上で作り直しが要る。クラゲは縦長に作り直すと更に安全になる
2. **実際に塗った紙を撮った写真を、1種類につき2〜3枚**

2 があれば、照合のしきい値を実測で決められる。
いまの暫定値（0.7）は自由に描いた絵3枚で測ったもので、
**本番（輪郭が台紙と同一）では当てはまらない可能性が高い**。

台紙を作るときの制約: **まわすと重なる形を2種類入れない**
（縦長と横長など）。形だけを見ているので原理的に区別できない。

---

## 手が空いたらやるもの（待ちなし）

| # | やること | なぜまだやらないか |
|---|---|---|
| T-5 | 下を泳ぐ絵が飾りに埋もれる件 | 飾りを高くした代償（F-329）。絵の後ろに淡い光を戻すか、絵が泳ぐ高さを上に寄せるかで直せる。**まず会場の画面で見てから決めたい** |
| T-6 | 宇宙テーマ（F-505） | 動きは水族館と同じ。背景だけ |
| T-7 | 森・草原テーマ（F-506・舞う動き） | `flutter` が未実装 |
| T-8 | 夜空テーマ（F-507・昇る動き） | `rise` が未実装 |

---

## 分かっていて直していないもの

| # | 事象 | 判断 |
|---|---|---|
| T-9 | HEIC（iPhone の既定形式）を読めない | 変換の仕組みを積むより、撮影時に「互換性優先」にしてもらうほうが軽い。警告は出している |
| T-10 | 起動直後に世界が1回作り直される | 設定を読み終える前に既定値で作るため。起動の一瞬だけなので実害は無い |
