# PaintingsAquarium — 開発の恒久指示

**作業を始める前に、必ず次の3つを読むこと。**

- `docs/要件定義.md` — 何を作るか、誰が使うか、**やらないこと**
- `docs/機能要項.md` — 実装した機能の台帳（`F-番号`）
- `docs/開発規則.md` — 恒久的に守る規則（`R-番号`）

**`R-番号` は、過去に実際に踏んだ失敗の記録です。必ず守ってください。**

---

## 態度

- 率直な意見を言う。反対することは反対してよい。ディベートする。
- 指示に問題があると思ったら、1〜2文で懸念を言ってから、**それでも作業は進める**。
  「それでいい」と言われたら、それ以上繰り返さず実行する。
- できたことは「できた」、落ちたテストは**出力ごと**「落ちた」と正直に書く。
  やっていない検証を「確認しました」と書かない。

## 誰のための道具か

| | |
|---|---|
| 実際に毎日これを触るのは誰か | **会場スタッフ**（および運営者1人） |
| その人が使う端末 | **PC 1台＋スキャナ**（アプリを触るのはPCのみ。絵は**スキャナで取り込む**） |
| その人が1日にこの操作を何回やるか | **スキャン 30〜100回／アプリ操作は 0 回**（スキャナの保存先が取り込みフォルダ） |

機能を作るときは、見た目より先に**その人の操作回数**を決めてから作る。
毎日・毎週繰り返す入力は、操作回数を減らす設計（初期値・一括操作・自動化）を必ず入れる。

> このプロダクトの核心は「スタッフのアプリ操作を0回にする」こと。
> 操作を1つ増やす提案は、それを壊していないか必ず確認する。

## 記録（最重要・毎回やること）

作業のたびに上記3ファイルを更新する。**コミット前に必ず更新すること。**

**`R-番号` は「失敗したこと」を残す場所。** バグを踏んだら、直して終わりにせず
「何が起きたか・なぜそうなったか・今後どうするか」の3つを `R-番号` で残す。
同じ失敗を繰り返さないための唯一の仕組み。

手順は `.claude/skills/feature-log/SKILL.md` にある。

## コードの書き方

- コメントは「何をしているか」ではなく**「なぜそうしたか」**を書く。
  特に、素直な実装を選ばなかったところには理由を必ず書く。
- 判定ロジック（透過のしきい値、トリミング範囲、泳ぐ軌道、重複判定など）は
  画面やファイルI/Oに埋めず、**純粋な関数として切り出して単体テストで固定する**。
- ユーザーに見せるエラーは「できません」で終わらせない。
  **何が原因で、どうすれば通るのか**まで書く。

## 検証

- typecheck / テスト / build を通してから「できました」と言う。
- 画面を変えたら、実際にアプリを起動して通しで確認する。
  DOMの有無ではなく、**本当にそのボタンが押せるか**まで見る。
- 検証のために邪魔な要素を消さない。**利用者が最初に見る状態のまま**試す。

## 安全

- 本番への反映、外部サービスへの送信、データの削除は、必ず確認してから。
- 個人情報（氏名・電話番号など）は持たない（`docs/要件定義.md` §5）。
- 外部サイトへの実書き込みは、環境変数のスイッチで止めた状態から始める。
- **このアプリは完全オフラインで動く。** 外部通信を足す変更は、
  それ自体が要件違反なので必ず確認を取る。

## 進め方

- ブランチを切って作業し、PRを作り、CIを通してからマージする。
- 大きい機能は、先に設計をドキュメントで見せる。
  決めるべきことがあれば、**実装を止めて質問にまとめる**。
  （選択肢を並べるだけでなく、**推奨も書く**）
